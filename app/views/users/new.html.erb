<% content_for(:title, t('.title')) %>
<section class="section-padding">
  <div class="container">
    <div class="row">
      <div class="col-lg-8 col-12 mx-auto">
        <h3 class="title-text text-center"><%= t('.title') %></h3>
        <div class="tab-content shadow-lg">
          <%= form_with model: @user, local: true do |f| %>
            <%= render 'shared/error_messages', object: f.object %>
            <div class="form-group mb-3">
              <%= f.label :last_name, class: 'required' %>
              <%= f.text_field :last_name, class: 'form-control', placeholder: t('defaults.placeholder.last_name') %>
            </div>
            <div class="form-group mb-3">
              <%= f.label :first_name, class: 'required' %>
              <%= f.text_field :first_name, class: 'form-control', placeholder: t('defaults.placeholder.first_name') %>
            </div>
            <div class="form-group mb-3">
              <%= f.label :email, class: 'required' %>
              <%= f.email_field :email, class: 'form-control', placeholder: t('defaults.placeholder.email') %>
            </div>
            <div class="form-group mb-3">
              <%= f.label :password, class: 'required' %>
              <%= f.password_field :password, class: 'form-control' %>
            </div>
            <div class="form-group mb-3">
              <%= f.label :password_confirmation, class: 'required' %>
              <%= f.password_field :password_confirmation, class: 'form-control' %>
            </div>
            <div class="text-center mb-3">
              <%= f.submit t('defaults.register'), class: 'btn btn-default new-btn btn-wide' %>
            </div>
          <% end %>
          <div class="py-3 d-flex justify-content-center">
            <%= link_to t('.to_login_page'), login_path, class: "link-text" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
