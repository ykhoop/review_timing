<tr>
  <th scope="row"><%= subject_detail.chapter %></th>
  <td><%= "#{subject_detail.start_page} ～ #{subject_detail.end_page}" %></td>
  <td><%= I18n.ln(subject_detail.start_at) %></td>
  <td>
    <div class="text-center">
      <%= link_to t('defaults.detail'), review_time_subject_detail_path(subject_detail), class: 'btn btn-default detail-btn' %>
      <%= link_to t('defaults.edit'), edit_subject_detail_path(subject_detail), class: 'btn btn-default edit-btn' %>
      <%= link_to t('defaults.delete'), subject_detail_path(subject_detail), data: { turbo_method: :delete, turbo_confirm: t('defaults.message.confirm_delete') }, class: 'btn btn-default del-btn' %>
    </div>
  </td>
</tr>
